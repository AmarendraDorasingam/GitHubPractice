@isTest
public class TestVerifyDate {
@isTest static void CheckDates() {
        date pri = VerifyDate.CheckDates(System.today(),System.today()+1);
        date pri1 = VerifyDate.CheckDates(System.today(),System.today()+60);
  
    }
   
}